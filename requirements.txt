# LLM Long-Term Memory (ENGRAM v2)
# Backend: FastAPI (serves REST + the no-build React frontend in ./frontend)
fastapi>=0.110
uvicorn>=0.29

# LLM providers
openai>=1.40            # Deepseek (OpenAI-compatible endpoint)
google-genai>=0.3       # Gemini (optional)

# Local embedding: EmbeddingGemma GGUF via llama.cpp (put the .gguf in ./model)
llama-cpp-python>=0.3

# data / env
numpy>=1.26
python-dotenv>=1.0
tzdata                  # zoneinfo has no system tz database on Windows
