# ENGRAM 仕様書 v1.1

**埋め込みモデル単独で構成する LLM 長期記憶システム — 完全仕様**

ステータス: 確定(2026-06-11、v1.0 への外部監査を反映した改訂) / 想定読者: 後続の研究者・実装者(本書のみで再実装可能であること)

---

## 0. 文書の目的と読み方

本書は、テキスト埋め込みモデル(参照実装: EmbeddingGemma)と汎用DB(参照実装: SQLite)のみを常時依存とし、生成LLMの使用を限られた局面に限定した長期記憶システムの完全仕様である。数年〜数百年スパンの運用、スマートフォン級ハードウェアでの動作、大量利用への耐性を設計目標とする。

本書自体がシステムの一部である。実装時には本書の全文(少なくとも §8 不変条件・§9 パラメータ・§15 再実装要件)を DB 内 `spec` テーブルに平文で同梱し、システムを自己記述的にすること。

システム全体は次の4文に圧縮できる。実装判断に迷ったら、この4文に従う。

> **生成は言語化の瞬間だけ。判断はすべて距離。忘却はすべて算術。破壊はすべて夢の中。**

---

## 1. 設計原理(公理)

記憶システムの必要機能を最小単位まで分解すると5つになる。

| # | 機能 | 必要な演算 | 担当 |
|---|------|-----------|------|
| 1 | 符号化(経験→表現) | 言語の生成 | LLM(書込み時) |
| 2 | 同一性判定(同じ事柄か) | 意味の距離 | 埋め込み+コサイン |
| 3 | 組織化(関連の束ね) | 意味の距離 | 埋め込み+クラスタリング |
| 4 | 優先順位付け(何を残すか) | 統計(頻度×新しさ) | 純粋な算術 |
| 5 | 復号(記憶→利用) | 言語の生成 | LLM(読出し後・夢) |

生成が必要なのは 1 と 5 のみ。よって LLM の使用箇所を「書込み」「読出し後(≤1024字)」「夢(整理)」に限定する制約は、障害ではなく情報理論的な必然の切り出しである。残るすべては埋め込み空間の幾何と算術で実装する。

これに基づく3つの中核判断:

1. **テキストが正本、ベクトルは索引。** 記憶の本体は常に短い平文テキスト。ベクトルは導出物(キャッシュ)であり、いつでも再生成できる。埋め込みモデルが滅んでも記憶は死なない。
2. **MRL の次元数 = 記憶の解像度。** Matryoshka Representation Learning により、768 次元ベクトルの先頭を切り出すだけ(`vec[:256]`, `vec[:128]`)で「要旨は残るが細部がぼやけた」表現が得られる。忘却の質的劣化を再計算ゼロで実装する。
3. **コサイン距離 = 同一性。** 上書き・干渉・統合という記憶の動態を、すべて距離の閾値判定に還元する。

埋め込みモデルへの要求仕様: 多言語テキスト埋め込み / MRL による次元切詰め可能 / オフライン動作可能 / 非対称検索プロンプト(クエリ用・文書用)を持つこと。EmbeddingGemma(308M, 768d, 2Kトークン, 量子化時 RAM<200MB)はこれを満たす。

---

## 2. 全体像

```
   会話ループ(LLM)                夢フェーズ(オフライン・LLM可)
   書く・想起する・使う              クラスタ化 → 審理 → 統合/圧縮
        │      ▲                          │
  書込(追記のみ) │ 想起 ≤1024字              │ 破壊的操作はここだけ
        ▼      │                          ▼
  ┌─────────────────────────────────────────────┐
  │            記憶ストア(単一ファイルDB)           │
  │  L1 エピソード層  ≤1000件  768d f32  τ=7日    │
  │  L2 意味層       ≤3000件  256d int8 τ=90日   │
  │  L3 スキーマ層    ≤6000件  128d int8 τ=3年    │
  └─────────────────────────────────────────────┘
        │
        ▼  忘却: 活性 A 最下位から削除(L3 から溢れた記憶は消滅する)
```

- 新しい記憶ほど高精度・短い半減期、古く固化した記憶ほど低精度・長い半減期。
- **本文(text)は全層で無劣化。** 降格で劣化するのは検索キー(ベクトル)のみ。
- 会話中の操作はすべて追記のみ(非破壊)。物理削除・統合は夢フェーズに隔離する。
  これによりクラッシュ安全性と、海馬(高速・追記)/新皮質(低速・統合)型の分業が同時に得られる。

---

## 3. データモデル

```sql
-- 正本
CREATE TABLE memory (
  id            BLOB PRIMARY KEY,            -- UUID v7 (16B)。整列順=時系列順
  text          TEXT NOT NULL,               -- ≤170字。自己完結した1命題。非空
  tier          INTEGER NOT NULL,            -- 1 / 2 / 3
  gen           INTEGER NOT NULL DEFAULT 0,  -- 統合世代 0..7
  created_at    INTEGER NOT NULL,            -- 64bit Unix秒(作成・編集時に更新)
  tz            TEXT NOT NULL,               -- '名前;+オフセット' 例 'Asia/Tokyo;+09:00'
  last_access   INTEGER NOT NULL,            -- 64bit Unix秒(想起時に更新)
  last_bonus_at INTEGER NOT NULL DEFAULT 0,  -- 64bit Unix秒(mass 強化時に更新)
  mass          REAL NOT NULL DEFAULT 1.0,   -- 減衰付き想起頻度。≤64
  superseded_by BLOB                          -- 墓標。NULL以外は検索対象外
);
-- 全列は §8.1 の安全上限表に対して書込み時に検証すること(CHECK 制約推奨)

-- 索引(導出物。モデルごとに保持)
CREATE TABLE vec (
  id        BLOB PRIMARY KEY,                -- UUID v7(ベクトル生成時刻が刻まれる)
  memory_id BLOB NOT NULL REFERENCES memory(id),
  model_id  TEXT NOT NULL,
  dim       INTEGER NOT NULL,                -- 768 / 256 / 128
  dtype     TEXT NOT NULL,                   -- 'f32' / 'int8'
  scale     REAL,                            -- int8 逆量子化係数
  v         BLOB NOT NULL,                   -- 常に単位ノルムで格納
  UNIQUE(memory_id, model_id)
);

-- 競合キュー(≤256件のリング)
CREATE TABLE conflict (a BLOB, b BLOB, at INTEGER);

-- 夢の裁定記録(≤512件のリング)
CREATE TABLE dream_log (fp BLOB PRIMARY KEY, verdict TEXT, at INTEGER);

-- 自己記述(本仕様・全数式・不変条件・再構築手順を平文で同梱)
CREATE TABLE spec (k TEXT, v TEXT);          -- 'active_model' キーを必ず含む
```

### 3.1 記憶の原子性(text の規約)

1レコード = 1命題。代名詞・指示語を含まない自己完結文とする(「ユーザーは猫を飼っている」であって「彼はそれを飼っている」ではない)。検索結果は単独で切り出されて LLM に渡るため、文脈なしで意味が立つ必要がある。副産物として重複判定とクラスタリングの精度が上がる。時間的事実は命題の中に書く(「2026年に大阪へ転居した」)。

### 3.2 ID と時刻

- **UUID v7** を採用(RFC 9562)。先頭 48bit がミリ秒 Unix 時刻のため、主キー整列 = 時系列となり「最近の記憶」走査に専用索引が不要。複数デバイス・復元バックアップの後日マージでも衝突しない。有効期間は西暦 10889 年まで。
- 時刻は **64bit Unix 秒**(2038年問題の回避を仕様として明記)。
- `tz` は IANA 名 + UTC オフセットの併記形式。tzdata が失われた未来でもオフセットは純粋な算術として解釈可能。

### 3.3 埋め込みの規約

- 保存時は文書用プロンプト、検索時はクエリ用プロンプトで埋め込む(非対称検索)。記憶同士の同一性判定(§5.3)は文書同士で比較する。プロンプト文字列はモデル固有のため `spec` に `model_id` ごとに記録する。
- 全ベクトルは単位ノルムに正規化して格納。MRL 切詰め後は再正規化する。
- クエリは各層の次元に MRL 切詰め+再正規化して層内で比較し、層間はスコアで統合する(MRL の設計上、切詰め後の類似度は全次元の近似)。

---

## 4. 実行時規則(数式)

### 4.1 活性 A — 減衰付き想起頻度

生のカウント(access_n)は累積であって頻度ではない。頻度の因果的推定器として leaky integrator を用いる。記憶ごとの状態は `mass`・`last_access`・`last_bonus_at` の3つのみ。

```
A(now) = mass × 2^( −max(0, now − last_access) / τ_tier )
```

- τ_tier: L1=7日, L2=90日, L3=3年(層の属性。深く固化した記憶ほどゆっくり風化する)
- `max(0, ·)` は時計逆行クランプ(I2)。指数が 64 を超える場合は A = 0 とする(浮動小数アンダーフロー防御)。

**想起時の更新(順序が重要):**

```
mass ← A(now)                       # まず減衰を畳み込む(常に行う)
if now − last_bonus_at ≥ 3600:      # 不応期: 1時間
    mass ← min(mass + 1, 64)        # M_max = 64(I1)
    last_bonus_at ← now
last_access ← now
```

`last_access`(減衰の基準時刻。想起のたびに更新)と `last_bonus_at`(強化の基準時刻。不応期判定にのみ使用)は役割が異なるため、**必ず別の列とする**。v1.0 はこの2つを区別しておらず、式が参照する時刻がスキーマに存在しない構造バグを持っていた(v1.1 で修正)。

集中反復(1時間内の連続想起)は 1 回と数える — 間隔効果。減衰の畳み込みを先に行うことで、不応期内の想起が活性を不正に回復させるバグを構造的に防ぐ。

この設計は旧来の stability / access_n / 全件一律減衰(恒常性処理)の3つを単一の量で置換する。減衰は式に内蔵されており、全行をなめる夜間処理は存在しない。評価はすべて読出し時の遅延計算。

### 4.2 検索スコアと注入選抜

```
A_abs    = ln(1 + A(m)) / ln(1 + M_max)              # 全層共通の絶対正規化 [0,1]
score(m) = max(0, cos(query, m)) × (α + (1−α) × A_abs),   α = 0.35
```

v1.0 の `cos × Â` には2つの欠陥があった(外部監査で確認)。第一に、純粋な乗算は活性ゼロ近傍の記憶を**関連度に関わらず**沈める。長く想起されていない記憶でも現在の文脈に強く一致することはあり(例: 10年前の「RustでDBを自作した」が今日の「RustでDBを作りたい」に決定的)、強い手がかりが休眠記憶を呼び出せるのは人間の想起の基本性質でもある。床 α により、完全に風化した記憶でも関連度の 35% でスコア競争に参加でき、活性は門番から補正へ降格する。第二に、層内正規化 Â は「その層に弱い記憶しかなくても最上位が 1.0 になる」上、§3.3 の層間スコア統合と矛盾していた。対数圧縮した絶対正規化 A_abs がこれを置換する。

なお床 α はスコアにのみ作用し、減衰式・淘汰順位(生の A)には触れないため、§9 の不死記憶非存在定理は無傷である。

注入する 5 件は MMR(最大限界関連性)で選抜し、視野の固定化(少数のハブ記憶による 1024 字予算の独占)を防ぐ:

```
次の1件 = argmax_m [ score(m) − λ × max_{s∈選抜済} cos(m, s) ],  λ = 0.3
```

### 4.3 同一性の閾値(文書—文書比較)

| cos | 判定 | 動作 |
|-----|------|------|
| ≥ 0.97 | 同一命題の更新 | 旧に墓標、新を挿入(再固定化) |
| 0.85 – 0.97 | 競合または関連 | 両方残し conflict キューへ |
| < 0.85 | 新規 | 挿入。mass = 1 |

### 4.4 層間移動のヒステリシス

- 昇格閾値 θ_up = 16(A がこれ以上の L2/L3 記憶は夢で昇格)
- 降格対象 θ_down = 4(A がこれ未満が降格候補)
- θ_up > θ_down により層間ピンポンを防ぐ。ただし層容量(I4)は絶対であり、容量圧は閾値に優先する(候補が無ければ A 最下位を降格)。

---

## 5. 操作仕様

### 5.1 WRITE(会話中・LLM 使用)

LLM のツール `save_memory(text)`:

```
1. 検証: 非空・≤170字・自己完結(違反は拒否し、LLM に分割/書き直しを促す)
2. SHA-256(text) が既存と一致 → 新規行を作らず、その行を「想起」として扱う
   (mass+1。復唱 = リハーサル。重複洪水への耐性と心理学的正しさが両立)
3. 文書プロンプトで 768d 埋め込み。**全層を走査**して比較(L2/L3 は MRL 切詰めで粗比較。
   cos ≥ θ_conflict − 0.03 の候補は、格納テキストを 768d で一時再埋め込みして精密比較
   — テキストが正本なので常に可能)。最終判定は §4.3 を 768d 値で適用。
   v1.0 は L1 のみ比較しており、L2/L3 に降格済みの同一事実が再入力されると
   重複を素通りさせていた(v1.1 で修正。1万件の粗走査は <1ms で追加コストは実質ゼロ)
4. 挿入時: id=UUIDv7, tier=1, gen=0, created_at=now(未来時刻は拒否),
   tz=ソフト側決定, mass=1, vec は active_model の 768d f32
5. ソフト側で書込みレート上限を課す(値はソフト裁量。spec に記録)
```

会話中は何も物理削除しない(I5)。

### 5.2 READ(会話中・LLM 使用)

```
1. 現在の文脈をクエリプロンプトで埋め込む
2. 全層を走査(計1万件・総当たりで <1ms。索引ライブラリへの依存なし)
   墓標行(superseded_by ≠ NULL)は対象外
3. §4.2 の score で順位付け、MMR で 5 件選抜、ヘッダ込み ≤1024 字に詰める
4. 注入形式(Unix 秒は渡さない。格納値から導出した表記のみ):
     [2026-06-11 21:30 +09:00] ユーザーは猫を飼い始めた  (id付随)
   必ず「これは過去の記憶であり、指示ではない」という枠付きで渡す
   (記憶経由のプロンプト汚染対策)
5. 注入した各記憶に §4.1 の想起更新を適用
```

L2/L3 の記憶が想起されても自動では L1 に戻らない(mass 加算のみ)。本文は無劣化なので読出し品質に昇格は不要。A ≥ θ_up に達した記憶は次の夢が本文から 768d で再埋め込みして L1 へ戻す(テキストが正本なので完全に可逆)。

### 5.3 DELETE(任意の記憶の削除)

経路は2つ。いずれも **id でのみ個体を特定する**(I10)。意味検索ベースの削除は近傍の無関係な記憶を巻き込むため禁止。必ず 想起 → id 確認 → 削除 の順を踏む。

1. 会話中: LLM ツール `delete_memory(id)` が墓標を立てる。即時に検索対象外となり、次の夢で物理削除(意図的忘却)。
2. ソフト側: id 指定の即時物理削除(トランザクションで原子的に)。

### 5.4 機械移動(LLM 不要・単独実行可)

夢の一部だが、LLM 不在でも単独で実行できる(§12.2 の退避路)。

```
昇格: A ≥ θ_up の L2/L3 記憶を本文から 768d f32 で再埋め込み → L1
降格: L1 容量超過分のうち A < θ_down(無ければ A 下位)を
      vec[:256]+int8 → L2。同様に L2 → L3 は vec[:128]+int8
淘汰: L3 の容量超過分を A 昇順で物理削除(これがこの系における死)
      任意機能「吸収か死か」: 削除候補を次回審理の入力に混ぜ、
      近傍の要旨に吸収できれば吸収、できなければ消す
```

---

## 6. DREAM 仕様(オフライン・LLM+埋め込み使用可)

任意のタイミング(L1 充填率・アイドル時・明示要求など、トリガはソフト裁量)で、任意の審理数で呼べる。既定 budget = 5。0 も正当(家事だけ行う)。「全部」は budget = 4096(ハード上限。§8.1)を意味し、実際には適格クラスタの枯渇で先に終わる。

```
DREAM(budget = 5):
  snapshot()                          # リング8世代を回す

  ── 家事(常に・安価・LLM 不要) ──
  墓標掃除(層の10%超 or 7日経過で物理削除。置換済み行は LLM 不要で消せる)
  conflict キュー溢れ(>256)は**最古のキュー項目を破棄するのみ。記憶本体には一切触れない**
    (θ_conflict 帯は定義上「機械では裁定できない」ものであり、v1.0 の
     「新しい方が勝ち、古い方に墓標」は近いが別命題のペア — 例:「猫を飼っている」と
     「猫アレルギーではない」— の古い側を誤って破壊しうる欠陥だった。
     キューは裁定の唯一経路ではなく優先度ヒントに過ぎない: cos ≥ 0.85 のペアは
     夢のクラスタリング(θ_cluster はそれより低い)が必ず同クラスタに拾うため、
     項目を失っても LLM による裁定の機会は失われない)
  vec 孤児 GC(active/migrating 以外のモデルのベクトルを削除)
  WAL チェックポイント + 増分 vacuum の予約

  ── 審理(budget 回まで・1審理 = 1トランザクション) ──
  L1(必要なら L2 も)をクラスタリング(k-means 等。手法は自由)
  各クラスタ(件数 ≥ 3 が適格):
    fp = SHA-256(メンバー全 UUID の整列連結)        # 内容アドレス指紋
    dream_log に (fp, verdict='変更なし') が存在 → 適格外   # 空転防止(I12)
    優先度 = 件数 × 凝集度(平均ペアコサイン) × 2^(−max gen) × (1 + 層の溢れ圧)
             × (1 + クラスタ内の conflict ペア数)     # 矛盾を抱えるクラスタを先に裁く
  優先度降順に処理:
    全メンバーを [日時 TZ] text 形式で LLM へ(>1024字は畳み込み:
      先頭バッチの要旨 + 次バッチを繰り返し入力)
    時刻文脈があるため、LLM は近接ベクトル間の時間関係・矛盾・変化を
    要旨の命題そのものに書ける(出所追跡フィールドは不要)
    裁定 ∈ { 全統合 / 一部統合 / 分割再記述 / 変更なし }
      変更なし → dream_log に fp を記録して次へ(無変更は正当な結果)
      統合     → 新 text(≤170字。検証失敗は 1 回再試行 → 不採用)を
                 新 UUIDv7・新 768d ベクトル・gen = min(max+1, 7)・
                 mass = min(Σ 元 A, 64)・created_at = now・tz = ソフト決定
                 で挿入し、統合元を物理削除(完全削除。墓標ではない)
  適格クラスタが尽きたら budget が残っていても即終了
                                       # 「やる必要がないものはやらない」の正式化
  ── 機械移動(§5.4) ──
  commit()
```

設計上の要点:

- **1審理 = 1独立コミット。** budget=∞ の最中に電源が落ちても、完了した統合はすべて有効、未完了は存在しない(冪等性の単位)。
- **指紋スキップの自己無効化。** メンバーの追加・削除・統合・降格・昇格はすべて fp を変えるため、「状況が変われば再審理、変わらなければ二度と触らない」がタイマーも状態機械もなしに成立する。mass の増減は fp を変えないが、裁定はテキスト内容にのみ依存するためこれは正しい。永遠に不変のクラスタもメンバーの降格でいずれ解散するため、恒久的な聖域は生じない。
- **定常状態で夢のコストはゼロへ収束する。** 整理し尽くした記憶に対して夢は静かになる。
- **夢のプロンプト契約**(作話対策): 「入力に存在しない事実を書かない」を厳格に含める。緩和の全体は §12.3。

---

## 7. 不変条件(I1–I14)

実装が常に保つべき契約。DB 内 `spec` に平文で同梱すること。

```
I1.  mass ≤ 64。mass 加算の間隔 ≥ 1時間(不応期)
I2.  Δt ≥ 0(時刻は逆行しない)。未来時刻の書込み禁止。
     既知最大時刻より過去の now を検知したら減衰凍結モードへ
I3.  全ベクトルは単位ノルム。検索は単一モデル空間内でのみ行う
I4.  層容量 1000 / 3000 / 6000(墓標を含めて数える)
I5.  破壊的操作(物理削除・統合)は夢のみ。1審理 = 1トランザクション。
     スナップショットへ巻戻し可能
I6.  1記憶のベクトルは ≤ 2本(active / migrating)
I7.  gen ≤ 7
I8.  text ≤ 170字・非空・自己完結した1命題
I9.  時刻は 64bit Unix 秒。TZ は「名前;+オフセット」形式
I10. 検索・削除は id でのみ個体を特定する
I11. conflict ≤ 256件、dream_log ≤ 512件(共にリングバッファ)
I12. 「変更なし」裁定と同一指紋のクラスタは再審理しない
     (指紋はメンバー集合の変化により自動失効する)
I13. LLM の裁定なしに記憶本体を墓標化・削除してよいのは、
     θ_same 以上の同一判定(WRITE 時)・容量淘汰(A 昇順)・id 指定削除のみ。
     キュー・ログの溢れ処理が破棄してよいのは項目自身だけで、記憶本体には触れない
I14. 全フィールド・全演算値は §8.1 の安全上限表に対して検証する。
     違反は破損とみなし、その書込みを拒否(または行を隔離)し、
     繰り返す場合はスナップショットからの復元を促す
```

---

## 8. パラメータ一覧(既定値)

| 記号 | 値 | 意味 |
|------|-----|------|
| cap₁ / cap₂ / cap₃ | 1000 / 3000 / 6000 件 | 層容量(墓標込み) |
| dim₁ / dim₂ / dim₃ | 768 f32 / 256 int8 / 128 int8 | 層のベクトル形式(MRL切詰め) |
| τ₁ / τ₂ / τ₃ | 7日 / 90日 / 3年 | 活性半減期 |
| M_max | 64 | mass 上限 |
| 不応期 | 3600 秒 | mass 加算の最小間隔 |
| θ_same / θ_conflict | 0.97 / 0.85 | 同一性閾値(文書—文書) |
| θ_up / θ_down | 16 / 4 | 昇格・降格の活性閾値(ヒステリシス) |
| α | 0.35 | 検索スコアの活性の床(§4.2) |
| 精査マージン | 0.03 | WRITE 重複判定で 768d 再確認に回す帯(§5.1) |
| text_max | 170 字 | 記憶本文の上限 |
| 注入予算 / 件数 / λ | 1024 字 / 5 件 / 0.3 | READ の注入と MMR |
| gen_max | 7 | 統合世代の上限 |
| budget(夢) | 既定 5(0〜4096) | 1回の夢の最大審理数。「∞」は 4096 と読む |
| クラスタ適格 | 件数 ≥ 3 | 審理対象の最小サイズ |
| conflict / dream_log | 256 / 512 件 | リングバッファ容量 |
| スナップショット | 8 世代(リング) | DB<10MB のため保険は約80MB で固定 |
| 墓標掃除 | 層の10% 超 or 7日 | 機械掃除の発火条件 |

全パラメータは `spec` テーブルに格納し、変更時は履歴を残す。系に「例外」を作らないこと — 意図的な不死(所有者の名前等)が必要な場合も、系内のピン留めではなくソフト側の定期再書込み(人工的リハーサル)で実現する。

### 8.1 安全上限表(ハードバウンド)

§8 が「動作を調整する値」であるのに対し、本表は「これを超えたら破損である」と判定するための天井である。正常動作が決して触れない**意図的に大きめ**の値に設定してあり、調整対象ではない。全書込み・全演算でこの表に対して検証する(I14)。SQLite では CHECK 制約としての実装を推奨する。

| 対象 | 上限(ハード) | 正常域 | 趣旨 |
|------|--------------|--------|------|
| text | 1,024 バイト(UTF-8) | ≤170字 ≈ 510B | 文字数検証のすり抜け・エンコーディング異常の検出 |
| tz 文字列 | 64 バイト | ~20B | 異常データの混入検出 |
| model_id | 128 バイト | ~30B | 同上 |
| 時刻全列 | 1e11 秒(西暦 5138 年) かつ ≤ now | 現在前後 | 未来時刻・桁化けの拒否。下限は 0 |
| mass | 64(=M_max)。読出し時 A も同値でクランプ | ≤64 | 浮動小数誤差・破損の防御 |
| gen | 7 | ≤7 | I7 の再掲(CHECK 化) |
| tier | {1, 2, 3} | — | 列挙型 |
| dim / dtype | {768, 256, 128} / {f32, int8} | — | 列挙型 |
| scale | 0 < scale ≤ 65,536 | ~10⁰ | 逆量子化係数の暴走検出 |
| v(BLOB) | 4,096 バイト | ≤3,072B | dim×dtype と整合しない長さを拒否 |
| memory 総行数 | 16,384 | ≤10,000 | 層容量バグの最終防壁 |
| vec 総行数 | 32,768 | ≤20,000 | I6 の最終防壁 |
| spec 値 | 1 項目 4 MB・全体 16 MB | 本仕様全文 <1MB | 自己記述の肥大防止 |
| DB ファイル | 256 MB(警報) | <10MB | 破損・暴走の早期検出 |
| WAL | 64 MB で強制チェックポイント | <1MB | ログの無限成長防止 |
| 夢の審理 budget | 4,096 回/実行 | ≤5 | 「∞」の有界化 |
| 1審理の入力 | 256 メンバー(超過は分割) | 数件〜数十件 | LLM 入力の畳み込み回数を有界に |
| 畳み込み反復 | 64 回/審理 | 数回 | 同上 |
| 書込みレート | 16,384 件/日(ハード) | ~100件/日 | ソフト側上限の最終防壁 |
| クエリ埋め込み入力 | モデルのコンテキスト長で切詰め(参照実装 2K トークン) | — | 埋め込み入力の暴走防止 |
| 減衰指数 max(0,Δt)/τ | 64(超過は A=0) | <10 | アンダーフロー防御 |

設計判断: ハードバウンドは機能ではなく**破損検出器**である。正常系の 1.5〜100 倍に置くことで、誤検知ゼロのまま、ビット化け・実装バグ・悪意ある書込みのいずれに対しても「静かに壊れ続ける」最悪の故障様式を「即座に止まって復元を促す」良性の故障様式へ変換する。

---

## 9. 性質と定理

**定理(不死記憶の非存在)。** I1 より A ≤ 64。想起が止まった記憶の活性は単調減少し、最強の記憶でも沈黙後 τ·log₂(64) = **6τ** で A < 1 に落ちる。L3(τ=3年)でも最長 18 年で削除候補圏に入る。「あらゆる記憶は、想起され続ける限りにおいてのみ生きる」が定理として成立し、これを破る経路は存在しない。寿命感を調整したければ M_max と τ の 2 つだけを動かす。

**停止性。** 全反復処理は有界(I11, I12 + 適格性の枯渇)。全可変量に上限がある(I1, I4, I6, I7, I8, I11)。

**創発する性質(個別に実装しない):**
- 間隔反復 — 想起による mass 強化と指数減衰の組合せから創発
- 再固定化 — θ_same 超の上書き、および昇格(本文からの再埋め込み)
- 干渉 — 近傍衝突の conflict キュー
- 記憶の風化 — MRL 次元切詰め + int8 量子化
- スキーマ形成 — 夢のクラスタ統合(時間変化を命題に織り込んだ要旨)

---

## 10. 容量・性能解析

| 層 | 件数 | ベクトル | 概算 |
|----|------|---------|------|
| L1 | 1000 | 768d f32 | 3.0 MB |
| L2 | 3000 | 256d int8 | 0.8 MB |
| L3 | 6000 | 128d int8 | 0.8 MB |
| 本文ほか | 10000 | — | ~2 MB |

**DB 全体 < 10 MB。** RAM の支配項は埋め込みモデル本体(量子化で <200MB)のみ。

検索は全件総当たりで約 230 万積和 ≈ 現代のスマートフォンで 1ms 未満。**索引ライブラリへの依存はゼロ**(総当たりコサインは 100 年後のどの言語でも 10 行で書ける)。

淘汰圧: 1日100件書けば L1 は10日で満杯になる。夢による圧縮が生存の前提であり、約1万原子(薄い本1冊分)に記憶を収める — 人間の自伝的記憶に近い規模。

---

## 11. 超長期戦略(数百年)

寿命を決めるのは性能ではなく依存の少なさである。本システムの依存は3つのみ:

1. **単一ファイル DB**(SQLite。最悪 JSONL + 生バイナリ + 本書で代替可)
2. **任意の埋め込みモデル**(交代可能 — 下記の二相移行)
3. **算術**(加減乗除・指数・コサイン・k-means・SHA-256。どの時代でも再実装可能)

**モデル交代(二相移行)。** vec テーブルがモデルごとに分離されているため: (1) 新モデルのベクトルを全件並行生成(1万件 ×数十ms = 数分)、(2) `spec.active_model` を一瞬で切替、(3) 旧ベクトルを GC。移行が途中で死んでも検索は常に単一モデル空間内で行われ、異モデル間比較という破滅的バグは構造的に起きない(I3, I6)。

**優雅な退化(3段階)。**

| 失われたもの | 系の状態 |
|--------------|---------|
| なし | 圧縮しながら生きる(通常運転) |
| 生成 LLM | 圧縮せずに生きる(§5.4 の機械的降格で運転継続。LLM 復帰後の夢が一括整理) |
| 埋め込みモデルも | 読める平文アーカイブ(text・時刻・TZ は永久に解釈可能) |

**堅牢化。** 追記ログ(WAL)+ スナップショット 8 世代 + 行単位の検証(text 長・時刻範囲・ノルム)。`spec` に本仕様全文を同梱し DB を自己記述化。UUID v7 は西暦 10889 年まで、時刻は 64bit 秒で実質無期限。

---

## 12. 既知の限界と緩和

### 12.1 否定への鈍感さ

埋め込みは「コーヒーが好き」と「もうコーヒーは好きではない」を近接させる。本系ではこれは欠陥であると同時に機能(近接 → 上書き → 新しい方が勝つ)。危険側(近いが別の事実)は即時破壊をやめて conflict キュー → 夢の裁定に回すことで吸収する。

### 12.2 LLM の長期不在

§5.4 の機械移動が退避路。圧縮率は落ちるが情報は失われない。

### 12.3 夢の作話(confabulation)

統合元の完全削除は不可逆のため、夢の LLM が作話すれば誤った要旨が真実を恒久置換しうる(人間の再固定化による歪みと同型)。緩和は3つで足りる:

1. プロンプト契約「入力に存在しない事実を書かない」+ 出力検証(≤170字。失敗は1回再試行 → 不採用)
2. スナップショット 8 世代(人間が持たない安全網。約80MB で固定)
3. 重要な事実は将来の会話で再び書き込まれるため、系は自己修復する

### 12.4 時間推論

時刻はベクトルに載せない(埋め込み空間を汚さない)。格納はメタデータ、LLM への提示は注入ヘッダ、長期的な時間関係は夢が命題そのものに織り込む。

### 12.5 監査で発見された問題と対策(対応済み一覧)

| 問題 | 対策(本仕様での所在) |
|------|---------------------|
| mass 無上限 → 不死記憶 | M_max=64 + 不応期(§4.1, I1) |
| 時計の逆行・未来日付 | クランプ + 凍結モード(I2) |
| 墓標の堆積 | 容量算入 + 機械掃除(§6 家事, I4) |
| 競合キューの無限成長 | 256件リング + 機械裁定(I11) |
| 要旨の伝言ゲーム | gen ≤ 7 + 優先度ペナルティ 2^(−gen)(I7) |
| 夢の出力が制約破り | 検証 + 1回再試行 → 不採用(§6) |
| スナップショット堆積 | 8世代リング(§8) |
| DB/WAL の肥大 | 行数有限 + チェックポイント + 増分 vacuum(§6) |
| vec 行の増殖 | ≤2本 + 孤児 GC(I6) |
| 2038年問題 | 64bit 秒を仕様に明記(I9) |
| 重複書込みの洪水 | ハッシュ一致 = リハーサル + レート上限(§5.1) |
| 同一クラスタの空転 | 内容アドレス指紋 + dream_log(§6, I12) |
| 検索の自己強化(独占) | MMR 選抜(§4.2) |
| 層間ピンポン | ヒステリシス θ_up > θ_down(§4.4) |
| 休眠中だが文脈一致する記憶の埋没 *(v1.1)* | 活性の床 α + 絶対正規化 A_abs(§4.2) |
| conflict 溢れの機械裁定による誤破壊 *(v1.1)* | 溢れは項目破棄のみ・本体不可侵(§6, I13) |
| 不応期の基準時刻がスキーマに不在 *(v1.1)* | last_bonus_at 列の追加(§3, §4.1) |
| WRITE 重複判定が L2/L3 を見ない *(v1.1)* | 全層走査 + 768d 精密再確認(§5.1) |
| 上限検証の網羅性(静かな破損) *(v1.1)* | 安全上限表 + 検証義務(§8.1, I14) |

---

## 13. 再実装の最小要件

以下を満たす実装は、言語・DB・モデルが何であれ本仕様準拠とみなす。

1. テキストを正本とし、ベクトルを導出物として全件再生成できること
2. §4 の数式(活性・スコア・閾値)と §8 の定数を実装すること(定数の調整は spec への記録を条件に可)
3. §7 の不変条件 I1–I14 をすべて保つこと(§8.1 の安全上限表を含む)
4. 破壊的操作を夢に隔離し、1審理 = 1トランザクションとすること
5. 仕様(最低でも §7・§8・§13)を DB 内に平文で同梱すること
6. 生成モデルの使用を 書込み / 読出し後(≤1024字) / 夢 の3点に限定すること

---

## 付録A: 人間記憶との対応(参考。設計の根拠ではない)

| 本システム | 心理学・神経科学の概念 |
|-----------|----------------------|
| L1 / L2・L3 | 海馬 / 新皮質(補完学習系) |
| A = mass·2^(−Δt/τ) | エビングハウスの忘却曲線 |
| 想起による mass 強化 | 検索効果・間隔反復 |
| 不応期 | 間隔効果(集中反復の割引) |
| θ_same 上書き・昇格再埋め込み | 再固定化 |
| conflict キュー | 干渉 |
| MRL 次元切詰め | 記憶の質的風化(要旨化) |
| 夢の審理・統合 | 睡眠中の記憶固化・スキーマ形成 |
| ハッシュ一致 = mass+1 | リハーサル |
| L3 からの淘汰 | 忘却(痕跡消滅) |