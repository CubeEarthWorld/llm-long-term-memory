[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q -m "not slow"
markers =
    slow: millennia simulation (minutes); run with  pytest -m slow
filterwarnings =
    ignore::DeprecationWarning
