# ENGRAM v2 仕様書

**痕跡（trace）モデルによる LLM 長期記憶** — Python (`llm-long-term-memory`) と Dart (`long_term_memory`) の共通仕様。両実装はこの文書の数式・手順・既定値を同一に実装し、共通のシナリオを再生して同一の実行トレースを得ることをテストで保証する（`long-term-memory/test/conformance/`）。

---

## 0. 目的

LLM は文脈窓の外を覚えられない。本系はその外側に「想起され続ける限り生きる」記憶を置く。設計目標は 4 つ。

1. **人間の記憶の原理に忠実**（忘却曲線・検索練習効果・間隔効果・情動による符号化強度・海馬バッファ・睡眠中の系統的固定化・再固定化・干渉の解消・痕跡の消滅）。
2. **汎用**（任意の埋め込みモデル・任意の DB・任意の LLM・任意のアプリ。ホストの負担は最小）。
3. **単純で美しい**（概念が少なく、機構が直交し、冗長な装置がない）。
4. **数千年の連続運転**に耐える（全状態が有界・演算コストが経過時間に依存しない・桁溢れなし・モデルや LLM を失っても優雅に退化する）。

## 1. 原理

記憶は**痕跡**である。痕跡は 2 つの数を持つ: いつ最後に想起されたか（`last_recall`）と、どれだけ安定か（`stability`、想起可能性の半減期）。想起可能性 `R` は安定度で決まる速さで指数減衰し、想起のたびに安定度が上がる。想起が困難であったほど（R が低いほど）、また手がかりが強く痕跡を活性化したほど、上昇は大きい。層・カウンタ・キュー・リングは無い。順位付け・淘汰・固定化状態・夢の優先順位は、すべてこの 2 つの数と 1 つのフラグから読み出す。**LLM なしに起こる破壊は「弱さによる忘却」だけである。**

生成 LLM の使用は 3 点に限る: 書込み時（何を覚えるか）・読出し後（想起の利用と引用）・夢（要旨化）。残りはすべて埋め込み空間の幾何と算術である。**テキストが正本、ベクトルは索引**（本文から常に再生成できる）。

## 2. データモデル（1 テーブル）

```
memory(
  id            ULID 形式（先頭 10 文字 = 50bit ミリ秒時刻、整列順 = 時系列。西暦 37,000 年まで有効）
  text          ≤170 文字。代名詞を含まない自己完結した 1 命題。区切り文字《》を含まない
  created_at    64bit Unix 秒（記憶が述べられた時刻）
  tz            'IANA名;+HH:MM'（例 'Asia/Tokyo;+09:00'。オフセット併記で tzdata 不要）
  last_recall   64bit Unix 秒（減衰の基準時刻）
  stability     秒（想起可能性の半減期）
  consolidated  真偽値（夢で処理済みか。false = 不安定/labile）
  model_id      ベクトルを生成した埋め込みモデル（'' = 未索引）
  vector        単位ノルム float32（model_id 下の索引。導出物）
)
```

ストアは全件を起動時に RAM へ読み込む（`capacity` 件以下。10,000 件 × (本文 + 768 floats) ≈ 35 MB。1 プロセスが 1 ストアを所有する）。ストアは耐久化の基盤に過ぎず、必要な操作は **`loadAll / put / remove / clear / transaction / backup`**（＋ open/close）。読込時に全行を検証・クランプする（`stability ∈ [1 s, S_max]`、`last_recall ≤ now`、`created_at ≤ now`）。`model_id` が現行モデルと異なる行は**未索引**であり、起動時と毎回の夢の冒頭でバッチ再埋め込みする。埋め込みが失敗しても本文は失われず、復旧するまで検索に現れないだけである（検索は常に単一モデル空間、起動は埋め込みモデルの有無で失敗しない）。

## 3. 忘却曲線と間隔効果（1 本の式・3 つの定数）

```
R(now)   = 2^( −max(0, now − last_recall) / stability )               想起可能性 ∈ [0, 1]
strength = stability × R(now)   (∝ ∫ R dt: 将来にわたる想起可能性の総量)
活性化    a = max(0, (cos − cosineFloor) / (1 − cosineFloor))          手がかりの活性化 ∈ [0, 1]
想起時    : stability ← min( stability × (1 + gain × a × (1 − R(now))), S_max );  last_recall ← now
新規痕跡  : stability = clamp(S0 × salience, 1 s, S_max);  last_recall = created_at = now
```

既定値: `S0 = 1 日`, `gain = 3`, `S_max = 10 年`, `salience ∈ [0, 10]`（API 境界でクランプ、既定 1）, `cosineFloor = 0`（EmbeddingGemma では ≈ 0.4: 無関係な文どうしのコサイン基準）。

- 直後の再想起（R ≈ 1）は安定度をほぼ増やさない — 集中反復の割引は不応期タイマなしで式から出る。
- ほぼ忘れかけた記憶を強い手がかりで想起（R ≈ 0, a ≈ 1）すると安定度は `1 + gain` 倍 — 望ましい困難。弱い手がかり（a = 0.3）はその 30% しか与えないため、曖昧な問い合わせで注入されたノイズが自己強化して不死化することはない。
- `salience` は符号化時の情動的重み（扁桃体による変調）であり、系内で唯一の例外機構。
- `2^(−巨大)` は浮動小数で正確に 0 になるため指数の上限は不要。淘汰順位は `log2(strength) = log2(S) − Δt/S` で比較し、アンダーフローによる同点を作らない。`S ≤ S_max` より、想起されない記憶は必ず想起される競合相手より下に落ちる: **不死の記憶は存在しない**。

## 4. 覚醒相（4 つの動詞）

### remember(text, salience)

1. 区切り文字《》を除去し、空白を畳み、170 文字を超える場合は文境界で切り詰める。空なら拒否。1 日あたりの書込み上限（`writesPerDay`）を課す。
2. 同一テキストが既にある → 想起として扱う（a = 1。リハーサル。新規行なし）。
3. 文書プロンプトで埋め込み、全件と総当たりコサイン。`best = max cos`。
4. 新規痕跡を挿入する。`consolidated = (best < θ_related)`（近傍がなければ統合対象がなく、生まれた時点で固定化済み）。**書込み時に上書き・削除は一切しない**: 言い換え・更新・矛盾はすべて不安定な対となり、夢が両方の時刻を見て審理する。
5. `best ≥ θ_related` なら、最近傍の既存痕跡も `consolidated = false` にする（**再固定化**: 再活性化された古い痕跡が不安定になる。訂正は古い事実の優先度で審理される）。
6. 件数が `capacity` を超えたら **猶予期間（3 日）を過ぎた痕跡のうち strength 最小のものを淘汰**する。猶予期間内の痕跡は保護されるが、その数が `capacity / 10` を超えたとき（洪水）は保護を解き、洪水は自身の成員を淘汰する。全件が猶予期間内なら全体から選ぶ。忘却はこの 1 行である。

### recall(query)

1. クエリを手がかり（行・文）に分割し（`maxCues` を超える分は連結して詰める。何も捨てない）、クエリプロンプトで埋め込む。痕跡ごとに `cos = max over cues`。
2. `score = a × (α + (1 − α) × R)`, `α = 0.35`（休眠中でも文脈に合う記憶は競争に残る）。
3. `score ≥ max(minScore, relativeScore × best score)` を残す（絶対床 0.1 = 何も合わなければ何も注入しない、相対閾値 0.6 = 強い当たりにノイズの尾を付けない）。MMR（`λ = 0.3`）で `injectN (5)` 件を選抜。`[unix tz] text 《id》` 形式で `budgetChars (1024)` 字以内に詰める（先頭 1 行は必ず入れる）。
4. 注入は**露出であって使用ではない**: 各痕跡に `a/2` の活性化で想起更新を適用する。

### cite(replyText)

回答テキスト中の《id:…》を直前の recall の注入集合と照合し、引用された痕跡の安定度を「a = 1 の完全な想起」に引き上げる（差分を乗算）。ホストが cite を配線しなければ既定で半分の強化になる。

### forget(id)

id のみで物理削除する（意味検索での削除は近傍の無関係な記憶を巻き込むため禁止）。

保守呼び出しは存在しない。容量は挿入のたびに守られ（書込みも夢も）、時計逆行の防御は式中の `max(0, Δt)` と読込時のクランプで足りる。

## 5. 睡眠相: dream(adjudicate, budget)

**系統的固定化**: 不安定な痕跡を既知の記憶と統合する。

```
backup()                                          （アダプタは世代リングを保持）
未索引の痕跡をバッチ再埋め込み（失敗しても未索引のまま残す）
seeds = consolidated = false の痕跡を安定度の高い順（証拠を多く持つものから統合。重要な事実の訂正は、その事実が種となって先に審理される）
各 seed について（調べる種は 8 × budget 件まで、budget が残る限り）:
   既に消費済み・固定化済み・同じ夢で LLM 失敗したクラスタの成員なら skip
   neighbours = cos(trace, seed) ≥ θ_related (0.75) の痕跡。近い順。≤ 7（クラスタ ≤ 8）
   if |cluster| < 2: seed.consolidated = true; continue        （LLM 不要・budget 消費なし）
   budget −= 1
   verdict = LLM( cluster を [local_time tz] text + R で提示, 現在時刻 )  →  keep | replace [texts]
   texts を清浄化（区切り除去・170 字・重複除去）。|texts| > |cluster| なら keep とみなす
   各 text を埋め込み、いずれかの成員とのコサインが gistMinCosine (0.5) 未満なら keep とみなす（作話・注入ガード）
   1 トランザクションで:
      keep    → 全成員を consolidated = true（強度は変えない: 露出は想起ではない）
      replace → 成員を削除し、各 text を consolidated = true の痕跡として挿入:
                stability   = min( max_i S_i + Σ_{i≠i*} strength_i, S_max )   （最強の成員 + 他の「生きた証拠」）
                R           = min(1, Σ strength_i / stability)               （クラスタの総強度を保存）
                last_recall = now − stability × min(64, −log2 R)            （固定化は想起可能性を膨らませない）
      その後に容量を守る
   （LLM 失敗はクラスタを未処理のまま残す ⇒ 次の夢で再試行）
```

- 近傍走査は競合キューが記録したであろう帯域と同じ閾値を使うため、矛盾と準重複は幾何だけで見つかる。キューは不要。
- クラスタは新しい痕跡がその近傍に着地したときだけ再訪される。整理し尽くした記憶に対して夢は静かになる — 指紋ログは不要で、夢のコストは `O(budget × capacity × dim)`。
- 1 回の誤った裁定の影響範囲は ≤ 8 行、毎回の夢の前にバックアップがある。

## 6. パラメータ（19）

| 名前 | 既定 | 意味 |
|---|---|---|
| capacity | 10000 | 痕跡の上限件数 |
| initialStability (S0) | 86400 s（1 日） | 新規痕跡の安定度 |
| spacingGain (gain) | 3.0 | 想起時の安定度上昇率 |
| maxStability (S_max) | 10 年 | 安定度の上限（不死の否定） |
| gracePeriod | 3 日 | 新規痕跡が淘汰から保護される固定化の窓 |
| cosineFloor | 0（EmbeddingGemma 0.4） | 無関係な文のコサイン基準（活性化の較正） |
| alpha | 0.35 | スコアにおける想起可能性の床 |
| injectN | 5 | 1 回の recall で注入する件数 |
| mmrLambda | 0.3 | MMR 多様性ペナルティ |
| minScore | 0.1 | 注入の絶対最小スコア |
| relativeScore | 0.6 | 最良スコアに対する相対閾値 |
| budgetChars | 1024 | 注入パックの文字予算 |
| maxCues | 8 | クエリ手がかりの上限 |
| thetaRelated | 0.75 | 夢の近傍閾値（不安定化の閾値） |
| dreamBudget | 5 | 1 回の夢の LLM 呼び出し上限 |
| dreamMaxMembers | 8 | 1 クラスタの最大成員（影響範囲） |
| gistMinCosine | 0.5 | 要旨が成員と持つべき最小コサイン（作話ガード） |
| textMax | 170 | 本文の最大文字数 |
| writesPerDay | 1000 | 書込みレート上限 |

閾値のうち `cosineFloor`・`thetaRelated`・`gistMinCosine` は埋め込みモデルのコサイン分布に依存する。モデルを替えたら較正すること。

## 7. 不変条件

- `0 ≤ R ≤ 1`、`1 s ≤ stability ≤ S_max`（生成時・更新時・読込時にクランプ）⇒ 不死の記憶なし。
- `Δt ≥ 0`（式中でクランプ）。全ベクトルは単位ノルム。検索は単一モデル空間（未索引の行は比較されない）。
- 各挿入後 `件数 ≤ capacity`（書込みも夢も）。
- 淘汰と `forget(id)` 以外、LLM の裁定なしに痕跡が消えることはない。不可逆な統合は夢のみ。1 クラスタ = 1 トランザクション、1 裁定 ≤ 8 行。
- `text` は非空・≤170 文字・区切り文字なし。時刻は 64bit 秒。削除は id のみ。
- 各痕跡は新しい証拠 1 つにつき高々 1 回だけ夢に見られる。

## 8. 数千年の挙動

全状態は有界（件数 ≤ capacity、S ≤ S_max、R ∈ [0,1]）。演算コストは経過時間に依らず、覚醒相は `O(capacity × dim)`、夢は `O(budget × capacity × dim)`。時間とともに成長するテーブルはない。id は 50bit ミリ秒（西暦 37,000 年）、時刻は 64bit 秒、ローカル時刻整形は西暦 9999 年を越えても純粋な算術で動く。古い埋め込みモデルは、モデルが使えるときに本文から癒される。LLM が不在でも固定化が止まるだけで、書込み・想起・強化・忘却は続く。10 年安定度の事実で飽和したストアでも、新しい記憶は猶予期間の間は必ず生き、その後は最も長く想起されていない古い事実が席を譲る。両実装の 3000 年シミュレーション（数十万回の書込み・想起・数万回の夢・10 年の沈黙・時計故障を含む）で全不変条件の成立と鍵となる事実の想起を確認している。

## 9. 人間の記憶との対応

| 本系 | 原理 |
|---|---|
| R = 2^(−Δt/S) | エビングハウスの忘却曲線 |
| 想起で S が伸びる | 検索練習効果 |
| 伸びが (1 − R) に比例 | 間隔効果・望ましい困難 |
| 伸びが手がかりの活性化 a に比例 | 部分的な再活性化は部分的な強化（符号化特定性） |
| 注入は a/2、引用で完全 | 想起とは提示ではなく使用である |
| salience | 情動（扁桃体）による符号化強度の変調 |
| 猶予期間 | 補完学習系の海馬バッファ |
| 不安定フラグ + 夢 | 海馬の再生 → 新皮質のスキーマ（系統的固定化） |
| 近い書込みで既存痕跡も不安定化し、原本は睡眠まで保存 | 再固定化（再活性化された痕跡が不安定になる） |
| 近傍クラスタリング | 睡眠中に解消される干渉 |
| 要旨は最強の成員 + 生きた証拠を継承 | スキーマは証拠の蓄積で強くなる（露出では強くならない） |
| strength 最小の淘汰 | 痕跡の消滅 |
| 符号化と利用の瞬間だけ言語化 | 生成は言語化の瞬間だけ |
